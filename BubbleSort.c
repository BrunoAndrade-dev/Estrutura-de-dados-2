#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void bubbleSort(int *vetor, int n) {
    for (int i = 1 ; i < n , i++) {
        for (int j = 0 ; j<n-1, j++){
            if (v[j]> v[j+1]){
                int x = v[j];
                v[j] = v[j+1];
                v[j+1] = x;
            }

        }
    }

}
